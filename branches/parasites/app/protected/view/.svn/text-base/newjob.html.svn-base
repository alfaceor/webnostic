<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
  <head>
  <!-- include 'header' -->
  </head>
  <body>
    <!-- include 'topbar' -->
    <!-- include 'navbar' -->
    <div id="content">
    <h1>Enviar {{jobtype}}</h1>
        <h2>Parametros Obligatorios</h2>
        <form id="jobform" method="post" action="{{action}}" enctype="multipart/form-data">

        <!-- if {{jobtype}} == sample -->
            
            <!-- if {{calibrations}} == NULL -->
            Usted <b>tiene</b> que tener un factor de calibracion, por favor envie una imagen de <a href="{{baseurl}}index.php/jobs/calibration/submit">calibracion</a>.
            <!-- else -->
            Suba una imagen de muestra: <input id="uploadedfile" name="uploadedfile" type="file"/><br /><br />
            Escoja un miscroscopio:
            <select name="calibration_value" id="calibration_value">
                <!-- loop calibrations -->
                <option value="{{calibrations' value.@value}}" />{{calibrations' value.@description}} - Value: {{calibrations' value.@value}}
                <!-- endloop -->
            </select>
            <input value="submit" type="submit" />
            <!-- endif -->
        <br />
        <!-- else -->
            Suba una imagen de calibracion: <input id="uploadedfile" name="uploadedfile" type="file"/><br /><br />
            Nombre de descripcion para el factor de calibracion: <input id="description" name="description" type="text"/><br /><br />
            <input value="submit" type="submit" />

        <!-- endif -->
        </form>
    </div>
  </body>
</html>
